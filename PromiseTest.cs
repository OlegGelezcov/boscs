using System.Collections;
using System.Collections.Generic;
using UnityEngine;
//using RSG;



public class PromiseTest : MonoBehaviour {

    //private readonly PromiseTimer promiseTimer = new PromiseTimer();

    //private IPromise WaitForSpace()
    //    => promiseTimer.WaitFor(.2f).Then(() => { promiseTimer.WaitUntil(timeData => Input.GetKeyUp(KeyCode.Space)); });

    ////private IPromise<string> Download(string url) {

    ////}

    //private void Update() {
    //    promiseTimer.Update(Time.deltaTime);
    //    WaitForSpace().Then(() => print("space pressed")).Then(() => print("xa-xa")).Done();
    //}

}
